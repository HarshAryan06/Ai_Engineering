# Day 1

A minimal Groq chat-completion example using Python and environment-based API configuration.

## Setup

1. Create a `.env` file containing `GROQ_API_KEY=your_api_key`.
2. Install dependencies with `uv sync`.
3. Run `uv run python hello_llm.py`.
